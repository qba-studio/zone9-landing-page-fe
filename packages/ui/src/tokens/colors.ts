export const baseColors = {
  scrollbar: '#e68842',
};

const buttonColors = {};

const textColors = {};

const tagColors = {};

export const lightColors = {
  ...baseColors,
  ...buttonColors,
  ...textColors,
  ...tagColors,
};

export const darkColors = {
  ...baseColors,
  ...buttonColors,
  ...textColors,
  ...tagColors,
};
